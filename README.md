# Movie-review-sentiment-RNN
This project implements sentiment analysis on IMDB movie reviews using an Embedding layer and Simple RNN in TensorFlow/Keras. The model converts text into dense vector representations and learns sequential patterns to classify reviews as positive or negative. Includes training code, preprocessing, and a Streamlit app.
